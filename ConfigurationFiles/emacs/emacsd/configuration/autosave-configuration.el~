(setq *auto-save-directory* (concat *base-configuration-module* "saves"))
(setq backup-directory-alist
      `((".*" . ,*auto-save-directory*)))
(setq auto-save-file-name-transforms
      `((".*" ,*auto-save-directory* t)))
